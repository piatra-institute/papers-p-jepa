[project]
name = "p-jepa-sim"
version = "0.1.0"
description = "Hidden-regime active sheaf predictive-state simulation for the P-JEPA paper"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.26",
    "matplotlib>=3.8",
]
